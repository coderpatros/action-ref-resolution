name: Test

on:
  workflow_dispatch

permissions: {}

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/setup-dotnet@a66eefa
      - uses: coderpatros/action-hello-world@7902eacf
